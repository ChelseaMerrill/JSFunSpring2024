import  prompt  from "readline-sync";
const num = prompt.question("please guess the number");
console.log(num);

//create a number at random (let or var)
//check that the number is between 1 and 10 (otherwise tell them to enter a number between 1 and 10)
//Tell the user if their guess is too high, too low, or correct
//Game over if the guess correctly (success message)
